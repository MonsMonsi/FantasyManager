<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    
                    xmlns:controls="clr-namespace:FantasyManager.WPF.Controls">

    <Style x:Key="LeagueContainer" TargetType="ListViewItem">
        <Setter Property="Height" Value="100"/>
        <Setter Property="Width" Value="200"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border Margin="10" Padding="19"
                            BorderBrush="{StaticResource BrushPrimary1}" BorderThickness="3" CornerRadius="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>

                                <Image Margin="5" Grid.Column="0" Grid.Row="0" Source="{Binding Flag}"/>

                                <TextBlock Margin="5, 20" Grid.Column="1" Grid.Row="0" Text="{Binding Name}"/>
                            
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style> 
</ResourceDictionary>