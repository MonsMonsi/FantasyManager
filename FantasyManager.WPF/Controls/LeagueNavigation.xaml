<UserControl x:Class="FantasyManager.WPF.Controls.LeagueNavigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:FantasyManager.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="Available Leagues:" Foreground="Azure" FontSize="20"/>

        <Border Grid.Row="1">
            <Border.Background>
                <SolidColorBrush Color="{StaticResource ColorPrimary2}" Opacity="0.8"/>
            </Border.Background>
            <Grid>

                <StackPanel>
                    <controls:LeagueCard DataContext="{Binding Leagues[0]}"/>
                    <controls:LeagueCard DataContext="{Binding Leagues[1]}"/>
                    <controls:LeagueCard DataContext="{Binding Leagues[2]}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
