<UserControl x:Class="FantasyManager.WPF.Controls.NameSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FantasyManager.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Border x:Name="rootBorder"
            Margin="5"
            Style="{StaticResource FeatureInnerBorder}">

        <TextBox Style="{StaticResource UserTeamNameTextBox}"
                 Text="{Binding UserTeamName, UpdateSourceTrigger=PropertyChanged}"/>
    </Border>
</UserControl>
