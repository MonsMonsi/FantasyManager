<UserControl x:Class="FantasyManager.WPF.Controls.TutorialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FantasyManager.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border x:Name="rootBorder"
            Margin="10"
            BorderBrush="{StaticResource BrushSecondary1}" BorderThickness="2" CornerRadius="20"
            Background="{StaticResource BrushPrimary3}">

        <TextBlock HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="10"
                   Text="{Binding TutorialMessage, UpdateSourceTrigger=PropertyChanged}"
                   FontSize="{StaticResource FontSizeBig}"/>
    </Border>
</UserControl>
