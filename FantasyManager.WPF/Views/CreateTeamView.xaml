<UserControl x:Class="FantasyManager.WPF.Views.CreateTeamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FantasyManager.WPF.Views"
             
             xmlns:controls="clr-namespace:FantasyManager.WPF.Controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="5" Padding="10" 
                    BorderBrush="{StaticResource BrushPrimary1}" BorderThickness="2" CornerRadius="10">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Choose a League"
                               Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="20"/>

                    <ListView ItemsSource="{Binding Leagues}" ItemContainerStyle="{StaticResource LeagueContainer}"
                              Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                              BorderThickness="0"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Margin="5" Padding="10" 
                    BorderBrush="{StaticResource BrushPrimary1}" BorderThickness="2" CornerRadius="10">

                <Grid>

                    <TextBlock Text="Choose a name for your Team"
                               Margin="0 40 0 0" HorizontalAlignment="Center" VerticalAlignment="Top"
                               FontSize="20"/>

                    <TextBox HorizontalAlignment="Center" VerticalAlignment="Center"
                             Height="25" Width="200"/>

                    <Button Content="Create Team"
                            Margin="0 0 0 10" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                            Height="25" Width="150"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Column="1" Margin="5" Padding="10" 
                BorderBrush="{StaticResource BrushPrimary1}" BorderThickness="2" CornerRadius="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ListView Grid.Column="0"/>

                    <Button Content="Join Season"
                            Grid.Column="1" Margin="0 0 0 50" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                            Height="25" Width="150"/>

                    <Button Content="Create Season"
                            Grid.Column="1" Margin="0 0 0 10" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                            Height="25" Width="150"/>
                </Grid>
                
            </Grid>
        </Border>
    </Grid>
</UserControl>
